[workspace]
members = [
    "server",
    "service",
    "pingora",
    "testing",
    "migration",
    "switchgear"
]

resolver = "2"


[profile.release]
lto = true
codegen-units = 1
panic = 'abort'
strip = true
